provider:
  name: gitea

resources:
  user:
    create:
      path: /admin/users
      method: POST
    read:
      path: /users/{username}
      method: GET
    update:
      path: /admin/users/{username}
      method: PATCH
    delete:
      path: /admin/users/{username}
      method: DELETE
    schema:
      attributes:
        aliases:
          username: id
  
  org:
    create:
      path: /orgs
      method: POST
    read:
      path: /orgs/{org}
      method: GET
    update:
      path: /orgs/{org}
      method: PATCH
    delete:
      path: /orgs/{org}
      method: DELETE
    schema:
      attributes:
        aliases:
          username: id

data_sources:
  user:
    read:
      path: /users/{username}
      method: GET
    schema:
      attributes:
        aliases:
          username: id
  
  org:
    read:
      path: /orgs/{org}
      method: GET
    schema:
      attributes:
        aliases:
          name: id
